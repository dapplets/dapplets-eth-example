{
  "name": "tutorial-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx hardhat run scripts/tutorial.js --network localhost",
    "compile": "npx hardhat clean && npx hardhat compile",
    "compile:run": "yarn compile && yarn deploy",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "account": "npx hardhat node"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.4",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.3",
    "hardhat": "^2.8.2"
  }
}
